[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "adam-ui"
version = "0.1.0"
description = "Modern AI Assistant UI"

[tool.setuptools.packages.find]
where = ["src"]
